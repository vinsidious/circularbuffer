{
  "name": "circularbuffer",
  "version": "0.0.0-dev",
  "description": "A small circular/ring buffer implementation in TypeScript.",
  "keywords": ["buffer", "circular", "queue", "ring", "stack", "typescript"],
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "https://github.com/vincecoppola/circularbuffer",
  "homepage": "https://github.com/vincecoppola/circularbuffer#readme",
  "author": {
    "name": "Vince Coppola",
    "email": "vincecoppola@gmail.com",
    "url": "https://github.com/vincecoppola"
  },
  "license": "MIT",
  "scripts": {
    "prepublish": "yarn run build",
    "lint": "tslint -p ./tsconfig.json --type-check",
    "test": "jest --config ./jest.json --coverage",
    "test:watch": "jest --config ./jest.json --coverage --watch",
    "prebuild": "rm -rf ./lib",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^20.0.7",
    "jest": "^20.0.4",
    "replication": "0.0.7",
    "ts-jest": "^20.0.10",
    "tslint": "^5.6.0",
    "tslint-config-ikatyang": "^2.2.0",
    "typescript": "^2.4.2"
  },
  "engines": {
    "node": ">= 6"
  },
  "files": ["/lib/**/*"]
}
